import { useEffect, useState } from 'react'
export default function Investments(){ const [deposits, setDeposits] = useState([]); useEffect(()=>{ fetch('/api/deposits').then(r=>r.json()).then(setDeposits) },[]); const approved = [{ id: 'a1', plan: 'basic', amount: 500, status: 'Approved', returns: 'R' + (500 * 2.5).toLocaleString() }] return (<div><h2 className="text-2xl font-bold mb-4">My Investments</h2><div className="space-y-3">{approved.concat(deposits).map(inv=> (<div key={inv.id} className="bg-white p-4 rounded shadow flex justify-between items-center"><div><div className="font-bold">{inv.plan ? inv.plan.toUpperCase() : 'Deposit'}</div><div className="text-sm">Amount: R{inv.amount.toLocaleString()}</div><div className="text-sm">Returns: {inv.returns ?? '—'}</div></div><div className="text-right"><div className="font-semibold">{inv.status}</div></div></div>))}</div></div>) }
