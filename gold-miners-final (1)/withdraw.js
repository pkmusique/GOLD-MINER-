import { useState } from 'react'
import { depositOptions } from '../lib/constants'
export default function Withdraw(){ const banks = ['FNB','Standard Bank','Nedbank','Absa','Capitec']; const [method,setMethod] = useState(banks[0]); const [amount,setAmount]=useState(100); const onRequest = ()=> alert('Withdrawal requested.'); return (<div className="max-w-2xl mx-auto"><h2 className="text-2xl font-bold mb-4">Withdraw</h2><div className="bg-white p-4 rounded shadow"><label>Select bank</label><select className="w-full p-2 border rounded mb-4" value={method} onChange={(e)=>setMethod(e.target.value)}>{banks.map(b=> <option key={b}>{b}</option>)}</select><label>Amount</label><select className="w-full p-2 border rounded mb-4" value={amount} onChange={(e)=>setAmount(Number(e.target.value))}>{depositOptions.map(a=> <option key={a} value={a}>R{a.toLocaleString()}</option>)}</select><div className="flex gap-3"><button onClick={onRequest} className="px-4 py-2 bg-amber-700 text-white rounded">Request Withdrawal</button></div></div></div>) }
